# Permalinks
permalink:           pretty

# Setup
title:               RESTful JSON
tagline:             Because adding links in JSON should be easy
url:                 http://restfuljson.org
baseurl:             ""

# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _sass
  style:            :compressed
